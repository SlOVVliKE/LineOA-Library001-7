# คู่มือขึ้นเซิร์ฟเวอร์ (Cloudflare Workers + Supabase Cloud + LINE)

> **อัปเดต ส.ค. 2569 — ย้ายจาก Netlify มา Cloudflare Workers แล้ว**
> เหตุผลและตัวเลขที่วัดได้อยู่ใน `แผนย้ายไป-Cloudflare-Workers.md`
> ขั้นตอน deploy ละเอียดอยู่ใน `คู่มือย้ายไป-Cloudflare.md`
> ไฟล์นี้เป็นภาพรวมทั้งระบบ

ทำตามลำดับ ข้ามขั้นไม่ได้ เพราะแต่ละขั้นต้องใช้ค่าจากขั้นก่อนหน้า

---

## ทำไมต้องมี 2 บริการ

**Supabase ไม่มีบริการโฮสต์เว็บ** — มีแต่ฐานข้อมูล / ล็อกอิน / เก็บไฟล์
ส่วนตัวเว็บ Next.js ต้องมีเซิร์ฟเวอร์รันแยกเสมอ

| บริการ | ทำหน้าที่ | ค่าใช้จ่าย |
|---|---|---|
| **Cloudflare Workers** | รันเว็บ (หน้าร้าน + หลังบ้าน + webhook + cron) | $5/เดือน |
| **Supabase** | ฐานข้อมูล ล็อกอิน เก็บสลิป/ปกหนังสือ | ฟรี ใช้เชิงพาณิชย์ได้ |

> **ทำไมไม่ใช้ Workers ฟรี** — แพ็กเกจฟรีจำกัด CPU 10 มิลลิวินาทีต่อ request
> ซึ่งไม่พอกับ SSR + auth ของเว็บนี้ (เอกสาร Cloudflare เองบอกว่างานแบบนี้กิน 10-20 ms)
> ถ้าฝืนใช้ ลูกค้าจะเจอ Error 1102 เป็นระยะ ซึ่งวินิจฉัยยากกว่าเว็บช้ามาก

---

## ขั้นที่ 1 — push โค้ดล่าสุดขึ้น GitHub

**โค้ดส่วน LINE ทั้งหมดยังไม่ได้ push** ถ้า deploy ตอนนี้จะได้เวอร์ชันเก่าที่ไม่มี webhook

ดับเบิลคลิก **`deploy-check-and-push.cmd`** ที่รากโฟลเดอร์
สคริปต์จะตรวจ type → build → push → **deploy ขึ้น Cloudflare** ให้ และหยุดถ้าขั้นไหนพัง

> **จุดที่เปลี่ยนไปตอนย้ายจาก Netlify** — เมื่อก่อน Netlify จะ build และ publish
> ให้อัตโนมัติทุกครั้งที่ push แต่ **Cloudflare ไม่ทำ** การ push ขึ้น GitHub เฉยๆ
> ไม่ทำให้เว็บที่ลูกค้าเห็นเปลี่ยนเลย ต้องมีขั้น deploy เสมอ
> (สคริปต์ทำให้แล้วในขั้นที่ 4 ของมัน)

---

## ขั้นที่ 2 — ย้ายฐานข้อมูลขึ้น Supabase Cloud

ฐานข้อมูลตอนนี้รันใน Docker บนเครื่อง — เซิร์ฟเวอร์ที่ Cloudflare มองไม่เห็น

1. เข้า <https://supabase.com/dashboard> → **New project**
   - Region เลือก **Southeast Asia (Singapore)** — ใกล้ไทยที่สุด ต่างกัน 150–200 ms ต่อคำสั่ง
   - ตั้ง Database Password แล้ว**เก็บไว้ให้ดี** หน้านี้แสดงครั้งเดียว
2. รอสร้างเสร็จ (~2 นาที) → **Project Settings → General** จด **Reference ID**
3. เปิด PowerShell ที่โฟลเดอร์ `book-shop` แล้วรัน:

```
npx supabase login
npx supabase link --project-ref <Reference ID ที่จดไว้>
npx supabase db push
```

`db push` จะรัน migration ทั้ง 17 ไฟล์ขึ้นฐานข้อมูลจริง

> **ข้อมูลตัวอย่างจะไม่ตามขึ้นไป** — `seed.sql` ตั้งใจให้รันเฉพาะเครื่องเรา
> เพราะมันสร้างบัญชีทดสอบ 6 บัญชีที่ใช้รหัสผ่านเดียวกันหมด
> ปล่อยขึ้นเซิร์ฟเวอร์จริงคือเปิดประตูทิ้งไว้ให้คนเดาเข้ามา

4. สร้างบัญชีแอดมินจริง: **Authentication → Users → Add user** (รหัสผ่านแข็งแรง)
   แล้วไป **SQL Editor** รันคำสั่งนี้ — **แก้แค่ 2 บรรทัดบนสุด**:

```sql
do $$
declare
  v_email text := 'อีเมลที่เพิ่งสร้าง';   -- <<< แก้ตรงนี้
  v_name  text := 'ชื่อคุณ';              -- <<< แก้ตรงนี้
  v_auth_id uuid;
  v_user_id uuid;
begin
  select id into v_auth_id from auth.users where email = v_email;
  if v_auth_id is null then
    raise exception 'ไม่พบ % ใน Authentication -> Users (ต้องสร้างบัญชีที่นั่นก่อน)', v_email;
  end if;

  insert into public.users (auth_user_id, email, display_name)
  values (v_auth_id, v_email, v_name)
  on conflict (auth_user_id) do update
    set email = excluded.email, display_name = excluded.display_name
  returning id into v_user_id;

  insert into public.user_roles (user_id, role_id)
  select v_user_id, r.id from public.roles r where r.code = 'owner'
  on conflict do nothing;

  raise notice 'ตั้งสิทธิ์เจ้าของร้านให้ % เรียบร้อย', v_email;
end $$;
```

**ตรวจว่าสำเร็จจริง** — รันต่ออีกคำสั่ง ต้องได้ 1 แถวและเห็น role `owner`:

```sql
select u.email, u.display_name, u.is_active,
       array_agg(r.code) as roles
from public.users u
left join public.user_roles ur on ur.user_id = u.id
left join public.roles r on r.id = ur.role_id
where u.auth_user_id is not null
group by u.id, u.email, u.display_name, u.is_active;
```

> **ทำไมต้องเป็น `auth_user_id` ไม่ใช่ `id`**
> ตาราง `public.users` มี `id` เป็นของตัวเอง แล้วผูกกับบัญชีล็อกอินผ่านคอลัมน์ `auth_user_id`
> แยกกันแบบนี้เพราะลูกค้าที่เข้ามาทาง LINE ไม่มีบัญชีใน `auth.users` เลย —
> มีแค่ `line_user_id` ตารางเดียวจึงต้องรองรับคนสองแบบ
>
> ถ้าเผลอใส่ `auth_user_id` ผิด ระบบจะ**ไม่ฟ้อง error แต่จะล็อกอินเข้าหลังบ้านไม่ได้**
> เพราะ `fn_me()` หาตัวตนจาก `auth_user_id = auth.uid()` — หาไม่เจอก็เท่ากับไม่มีสิทธิ์อะไรเลย
> นี่คือเหตุผลที่ต้องรันคำสั่งตรวจข้างบนด้วย

5. จด 3 ค่าจาก **Project Settings → API**:
   - Project URL
   - anon public key
   - **service_role key** — ค่านี้ข้าม RLS ได้หมด ห้ามใส่ในโค้ดฝั่งเบราว์เซอร์เด็ดขาด

---

## ขั้นที่ 3 — สร้างเว็บบน Cloudflare Workers

ขั้นตอนเต็มพร้อมเช็คลิสต์ทดสอบอยู่ใน **`คู่มือย้ายไป-Cloudflare.md`** ย่อไว้ตรงนี้

1. สมัคร <https://dash.cloudflare.com> → Workers & Pages → อัปเป็น **Workers Paid ($5/เดือน)**
2. `cd book-shop` แล้ว `npx wrangler login`
3. ใส่ secret ทีละตัวด้วย `npx wrangler secret put <ชื่อ>` ตามตารางข้างล่าง
   (พิมพ์ค่าในเทอร์มินัล ไม่ลงไฟล์ ไม่ผ่านแชท)
4. `npm run cf:deploy`

> **ไม่มีการเชื่อม GitHub แบบ auto-deploy** ต่างจาก Netlify เดิม
> ทุกครั้งที่อยากให้เว็บจริงเปลี่ยน ต้องสั่ง deploy เอง
> หรือดับเบิลคลิก `deploy-check-and-push.cmd` ซึ่งทำให้ครบทั้ง push และ deploy

### Environment variables

ตั้งสองที่ให้ตรงกัน — `wrangler secret put` (ตอนรัน) และ `.env.local` (ตอน build)
ตัวที่ขึ้นต้น `NEXT_PUBLIC_` **ถูกฝังตอน build** จึงต้องถูกต้องใน `.env.local` ด้วย

| ชื่อ | ค่า | หมายเหตุ |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Project URL จากขั้นที่ 2 | |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | anon public key | |
| `SUPABASE_SERVICE_ROLE_KEY` | service_role key | **ห้ามหลุด** |
| `NEXT_PUBLIC_APP_URL` | `https://libraryforu.thirakan-weef64.workers.dev` | ห้ามมี `/` ปิดท้าย |
| `LINE_CHANNEL_SECRET` | **Messaging API** channel ReadUP (`2011303337`) → Basic settings | ใช้ตรวจลายเซ็น webhook |
| `LINE_CHANNEL_ACCESS_TOKEN` | **Messaging API** channel → Messaging API tab | กด Issue แบบ long-lived |
| `LINE_LOGIN_CHANNEL_ID` | `2011334191` | **LINE Login** channel (ReadUPLiff) ไม่ใช่ Messaging API |
| `NEXT_PUBLIC_LIFF_ID` | `2011334191-2F8gRIx0` | คัดลอกจากหน้า LIFF อย่าพิมพ์เอง |
| `SHOP_ACCOUNT_NAME_TH` | ชื่อบัญชีที่แสดงตอนโอน | |
| `SLIP2GO_SECRET` / `SLIP2GO_ACCOUNT_TYPE` | ตรวจสลิปอัตโนมัติ | trial หมดอายุแล้ว เว้นว่างได้ |
| `PROMPTPAY_ID` | เบอร์พร้อมเพย์ร้าน | เลข 10 หรือ 13 หลัก ไม่ต้องมีขีด |
| `CRON_SECRET` | สุ่มเอง | กันคนนอกยิง endpoint cron |
| `APP_SECRET` | สุ่มเอง | |

สุ่มค่าลับด้วยคำสั่งนี้ใน PowerShell (รันสองครั้ง):

```
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

> **ตัวแปรที่ขึ้นต้นด้วย `NEXT_PUBLIC_` จะถูกฝังลงโค้ดฝั่งเบราว์เซอร์**
> ใครก็เปิดดูได้ — ห้ามเอา service_role key หรือ LINE token ไปใส่ชื่อแบบนั้นเด็ดขาด
> ที่ตั้งชื่อแบบนี้ได้เฉพาะ anon key กับ LIFF ID เพราะสองตัวนั้นออกแบบมาให้เปิดเผยอยู่แล้ว
> (anon key ปลอดภัยเพราะมี RLS คุมอยู่อีกชั้น)

หลัง deploy รอบแรกจะได้ URL `https://libraryforu.<ชื่อบัญชี>.workers.dev`
กลับไปแก้ `NEXT_PUBLIC_APP_URL` ให้เป็น URL จริง **ทั้งใน `.env.local` และ secret**
แล้ว `npm run cf:deploy` อีกรอบ

> ต้องแก้ทั้งสองที่เพราะ `NEXT_PUBLIC_APP_URL` ถูกใช้ใน `src/lib/line/flex.ts`
> เพื่อประกอบลิงก์ในข้อความ LINE ที่ส่งหาลูกค้า และ Next ฝังค่านั้นตอน build
> ถ้าแก้แค่ secret ปุ่มในข้อความ LINE จะยังพาไปที่อยู่เก่า

---

## ขั้นที่ 4 — ตั้งค่าใน LINE Developers Console

ที่ <https://developers.line.biz/console/>

**Messaging API channel → Messaging API tab**

- **Webhook URL** = `https://โดเมนของคุณ/api/line/webhook`
- กด **Verify** ต้องขึ้น Success
- เปิด **Use webhook**
- **ปิด Auto-reply messages และ Greeting messages** — ไม่งั้นลูกค้าได้ข้อความซ้ำสองรอบ
  (ของ LINE หนึ่ง ของระบบเราอีกหนึ่ง)

**LINE Login channel → LIFF** — ✅ ทำแล้ว ไม่ต้องทำซ้ำ

สร้างไว้ให้แล้วในชื่อ **ReadUPLiff** (Channel ID `2011334191`)

| ค่า | ที่ตั้งไว้ |
|---|---|
| LIFF app name | ร้านหนังสือ ReadUP |
| LIFF ID | `2011334191-2F8gRIx0` |
| LIFF URL | `https://liff.line.me/2011334191-2F8gRIx0` |
| Endpoint URL | `https://libraryforu.thirakan-weef64.workers.dev/shop` |
| Size | Full |
| Scopes | `openid` + `profile` |
| Add friend option | On (Normal) |

**ยังเหลืออีก 1 อย่างที่ Login channel** — ไปที่ **Basic settings → Linked LINE Official Account**
แล้วผูกกับ OA ตัวเดียวกับ Messaging API channel
ถ้าไม่ผูก ตัวเลือก "เพิ่มเพื่อน" ที่ตั้งไว้จะไม่ทำงาน และลูกค้าที่เปิดร้านจากลิงก์ภายนอก
จะไม่ได้เป็นเพื่อนกับ OA → **ส่งแจ้งเตือนหาเขาไม่ได้เลย** เพราะ LINE ห้าม push หาคนที่ไม่ได้เป็นเพื่อน

**เช็คว่าเชื่อมติดแล้ว:** เปิด `https://โดเมนของคุณ/api/line/webhook` ในเบราว์เซอร์
ต้องได้ `{"ok":true,"configured":true}`
ถ้า `configured` เป็น `false` = ใส่ key ไม่ครบ หรือยังไม่ได้ deploy ใหม่หลังใส่

---

## ขั้นที่ 5 — Rich Menu

ทำรูปขนาด **2500 × 1686 px** แบ่ง 6 ช่อง (3 คอลัมน์ × 2 แถว)
เรียง: หนังสือทั้งหมด / มาใหม่ / เปิดจอง / ตะกร้า / ออเดอร์ของฉัน / ติดต่อแอดมิน

บันทึกเป็น `richmenu.png` ในโฟลเดอร์ `book-shop` แล้วรัน:

```
node --env-file=.env.local scripts/setup-richmenu.mjs richmenu.png
```

---

## ระบบแจ้งเตือนทำงานยังไง

**ไม่ได้พึ่ง cron** — แจ้งเตือนถูกส่งทันทีตอนแอดมินกดยืนยันเงิน / บันทึกพัสดุ / รับของเข้า
ลูกค้าได้รับภายในไม่กี่วินาที

เบื้องหลังคือ trigger ในฐานข้อมูลเขียนข้อความลงตาราง `notifications` ก่อน
แล้วโค้ดจึงค่อยส่งออก ทำแบบนี้เพราะสถานะออเดอร์ถูกเปลี่ยนจากหลายที่
ถ้าให้แต่ละที่ส่ง LINE เอง สักวันจะลืมสักที่แล้วลูกค้าไม่ได้รับโดยไม่มีใครรู้

`wrangler.jsonc` → `triggers.crons` ตั้งงานตามเวลาไว้ 2 ตัวเป็นตาข่ายรองรับ
โดยมี `custom-worker.ts` เป็นตัวรับสัญญาณแล้วเรียก route ที่ถูกต้อง:

| ฟังก์ชัน | ความถี่ | ทำอะไร |
|---|---|---|
| `cron-notifications` | ทุกชั่วโมง | เก็บข้อความที่ส่งพลาด (เช่น LINE ล่มตอนนั้น) มาส่งซ้ำ |
| `cron-expire-reservations` | วันละครั้ง | ลบการจองสต็อกที่หมดอายุออกจากตาราง |

ตัวที่สองไม่ต้องรันถี่ เพราะทุกจุดที่คำนวณของคงเหลือกรอง `expires_at > now()` อยู่แล้ว
แถวที่ค้างจึงไม่เคยทำให้ยอดผิด แค่ทำให้ตารางบวมเฉย ๆ

ดูผลได้ที่เมนู **แจ้งเตือน** ในหลังบ้าน — บอกว่าส่งสำเร็จ/ล้มเหลวเพราะอะไร

---

## ค่าใช้จ่ายและข้อจำกัด

| บริการ | ฟรีถึงไหน | เมื่อไหร่ต้องจ่าย |
|---|---|---|
| Cloudflare Workers Paid | $5/เดือน · รวม 10 ล้าน request + CPU 30 ล้าน ms | ร้านขนาดนี้ไม่มีทางใช้เกิน |
| Supabase Free | 500 MB DB · 1 GB storage · ใช้เชิงพาณิชย์ได้ | **โปรเจกต์ถูกพักถ้าไม่มีคนใช้ 7 วัน** → Pro $25/เดือน |
| LINE OA | push 300 ข้อความ/เดือน | เกินต้องขึ้นแพ็กเกจ (ข้อความ reply ไม่นับโควตา) |

> **เรื่องที่ต้องระวังที่สุด: Supabase พักโปรเจกต์เมื่อไม่มีคนใช้ 7 วัน**
> ช่วงเปิดร้านใหม่ที่ยังไม่มีลูกค้าเข้ามาทุกวัน มีสิทธิ์โดนพัก แล้วเว็บจะล่มทั้งระบบ
> ปลุกกลับมาได้เองจาก dashboard แต่ต้องรู้ตัวก่อน
> วิธีกันคือให้มีอะไรแตะฐานข้อมูลอย่างน้อยสัปดาห์ละครั้ง —
> ซึ่ง cron ส่งแจ้งเตือนที่รันทุกชั่วโมงทำหน้าที่นี้ให้อยู่แล้วโดยบังเอิญ
>
> **ข้อควรระวังตอนย้ายโฮสต์** ถ้าวันไหนเปิด Netlify กลับมาโดยไม่ปิด cron ฝั่ง Cloudflare
> จะมีสองระบบไล่ล้างกล่องแจ้งเตือนเดียวกัน = ลูกค้าอาจได้ข้อความซ้ำ
> และสองระบบชิงกันปลดจอง pre-order — เปิดได้ทีละที่เท่านั้น

> LINE ให้ push ฟรีแค่ 300 ข้อความ/เดือน — ระบบเราแจ้งเตือนออเดอร์ละประมาณ 3 ข้อความ
> (ยืนยันเงิน + ส่งของ + ของถึง) แปลว่ารองรับได้ราว **100 ออเดอร์/เดือน**
> ส่วนบอทที่ตอบคำถามลูกค้าใช้ reply ซึ่งไม่นับโควตา จึงคุยได้ไม่จำกัด
