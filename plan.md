# แผนงาน — รื้อโครงสร้าง UX/UI หลังบ้าน

> เอกสารนี้คือ**แผนที่ใช้ทำงานจริง** ทำถึงไหนแล้วให้ติ๊กในตารางท้ายไฟล์
> เหตุผลเบื้องหลังและสภาพปัญหาอยู่ใน [`เอกสาร/6-แผนรื้อ-UX-หลังบ้าน.md`](./เอกสาร/6-แผนรื้อ-UX-หลังบ้าน.md)

**ขอบเขต** หลังบ้าน 16 หน้า / 27 ไฟล์
**ไม่แตะ** หน้าร้าน · ตรรกะธุรกิจ (FIFO ต้นทุน RLS) · ไม่ลง UI library ใหม่ · ไม่ทำโหมดมืด

---

## ข้อตกลงที่ตัดสินแล้ว

| # | เรื่อง | ตัดสินว่า |
|---|---|---|
| 1 | มือถือ | **รองรับด้วย** — เมนูข้างยุบเป็นปุ่มเมื่อจอแคบ |
| 2 | ตัวเลขงานค้าง | **view เดียวตอบทุกตัวเลข** ยิง query รอบเดียวต่อการโหลดหน้า |
| 3 | หน้ารายงานกำไร | **ปรับแค่ layout** ไม่แตะเนื้อหาและไม่แยกหน้า |
| 4 | สีป้ายสถานะออเดอร์ | **ทำชุดสีแยกเฉพาะหลังบ้าน** ห้ามแก้ `src/lib/orderStatus.ts` — ไฟล์นั้นใช้ร่วมกับ `/shop/orders` แก้ตรงนั้นจะผิดกฎ "ไม่แตะหน้าร้าน" |

---

## รอบ 1 — โครงหลัก + งานวันนี้

### 1.1 สร้าง view นับงานค้าง

ไฟล์ใหม่ `book-shop/supabase/migrations/0026_admin_work_queue.sql`

```sql
create or replace view public.v_admin_work_queue as
select
  (select count(*) from public.payments
     where verify_status = 'pending')                       as slips_pending,
  (select count(*) from public.orders
     where status = 'pending_payment')                      as orders_awaiting_payment,
  (select count(*) from public.orders
     where status in ('paid','packing'))                    as orders_to_ship,
  (select count(*) from public.orders
     where status = 'awaiting_balance')                     as orders_awaiting_balance,
  (select count(*) from public.preorder_queue
     where status in ('waiting','partially_filled'))        as preorders_waiting,
  (select count(*) from public.notifications
     where status = 'failed')                               as notifications_failed,
  (select count(*) from public.notifications
     where status = 'queued')                               as notifications_queued;

-- สำคัญ: ต้องเป็น security_invoker ไม่งั้น view จะรันด้วยสิทธิ์เจ้าของ
-- แล้วข้าม RLS ทั้งหมด กลายเป็นว่าใครก็ตามที่เข้า /admin ได้จะเห็นตัวเลข
-- ของทั้งร้าน แม้ไม่มีสิทธิ์ดูออเดอร์ (เคยตั้งแบบนี้ไว้กับ view อื่นแล้ว)
alter view public.v_admin_work_queue set (security_invoker = true);
```

**ค่าสถานะข้างบนดึงมาจาก CHECK constraint จริงในฐานข้อมูล ไม่ได้เดา**
`orders`: pending_payment · paid · preorder_waiting · awaiting_balance · packing · shipped · delivered · completed · cancelled
`payments.verify_status`: pending · auto_verified · manual_verified · rejected
`preorder_queue`: waiting · partially_filled · filled · cancelled
`notifications`: queued · sent · failed · skipped

- [x] เขียน migration — `0026_admin_work_queue.sql` ใช้ `with (security_invoker = on)` ตามธรรมเนียมไฟล์อื่นในโปรเจกต์
- [ ] `db-push.cmd` — **ยังไม่รัน** เพราะพุชขึ้น Supabase Cloud จริง ต้องให้คุณกดเองหรือบอกให้ทำ
- [x] ทดสอบด้วยบัญชีลูกค้าว่า RLS ยังกันอยู่ — จำลองด้วย `set local role authenticated; set local request.jwt.claim.sub = ...` ใน transaction ที่ rollback ทิ้ง:
  ใส่ออเดอร์ทดสอบสถานะ `pending_payment` แล้วเทียบมุมมอง — เจ้าของร้านเห็น `orders_awaiting_payment = 1` (ค่าจริง) ส่วนบัญชีลูกค้าเห็น `0` แถวเดียวกัน ยืนยันว่า RLS ยังกรองอยู่ ไม่ใช่วิวข้าม RLS

### 1.2 เมนูข้าง 5 กลุ่ม + รองรับมือถือ

แก้ `src/app/admin/layout.tsx`

```
ขาย      คำสั่งซื้อ ③ · สั่งจอง ①
สินค้า    หนังสือ · หมวดหมู่ · ลูกค้าสนใจ
คลัง      สต็อกและต้นทุน
รายงาน    กำไร
ระบบ      แจ้งเตือน ①
```

จาก 11 เหลือ 8 — `รับสินค้าเข้า` และ `ปรับสต็อก` ย้ายไปเป็นปุ่มในหน้าคลัง (รอบ 3)

**พฤติกรรมตามขนาดจอ**

| จอ | เมนู |
|---|---|
| ≥ 1024px | เมนูข้างค้างไว้ตลอด กว้าง 220px |
| < 1024px | ยุบซ่อน มีปุ่มเปิด — เลื่อนออกมาทับเนื้อหา ปิดได้ด้วยการแตะพื้นหลัง |

**ข้อควรระวัง** เมนูบนมือถือต้องปิดเองเมื่อกดลิงก์ ไม่งั้นจะค้างทับหน้าใหม่

- [x] เมนูข้างจัดกลุ่ม + ตัวเลขงานค้าง — `src/components/admin/Sidebar.tsx` (client component รับ nav groups เป็น props จาก layout) ตัวเลข "คำสั่งซื้อ" รวม slips_pending + orders_to_ship + orders_awaiting_balance เป็นก้อนเดียว (หน้า "งานวันนี้" แยกให้ดูทีละแถวอยู่แล้ว)
- [x] ยุบ/กางบนจอแคบ + ปิดเองเมื่อกดลิงก์ — ทดสอบจริงที่ 375px ด้วย `run-book-shop` driver แล้ว: เปิดแผง → กดลิงก์ → ปิดเองและเปลี่ยนหน้าถูกต้อง
- [x] ปุ่มเปิดเมนูสูงอย่างน้อย 44px — ตั้ง `minHeight/minWidth: 44` ตรงๆ
- [x] ตัวเลขเรียกจาก view ครั้งเดียวใน layout — `src/lib/admin/workQueue.ts` ห่อด้วย `cache()` ของ React แบบเดียวกับ `getCurrentUser` (layout + หน้าแรกเรียกฟังก์ชันเดียวกัน ยิง query จริงครั้งเดียวต่อ request)

**ทดสอบสิทธิ์แล้ว**: บัญชี `packer` เห็นแค่กลุ่ม "ขาย" (คำสั่งซื้อ/สั่งจอง ไม่มีตัวเลขค้างเพราะ seed ไม่มีงานค้าง) กับ "ระบบ" (แจ้งเตือน) ไม่เห็น สินค้า/คลัง/รายงาน เลย — ตรงกับสิทธิ์เดิมทุกประการ

### 1.3 หน้าแรกใหม่ — "งานวันนี้"

แทน `src/app/admin/page.tsx`

เรียงตามความเร่งด่วน ไม่ใช่ตามหมวดข้อมูล

```
งานวันนี้
  🔴 สลิปรอตรวจ              n ใบ      [ดู →]
  🔴 แจ้งเตือนส่งไม่สำเร็จ      n รายการ  [ดู →]
  🟡 ชำระแล้ว รอส่ง           n ออเดอร์  [ดู →]
  🟡 ของเข้าแล้ว รอจ่ายคิว      n คน      [ดู →]
  🟡 รอชำระส่วนที่เหลือ         n ออเดอร์  [ดู →]

  ถ้าทุกตัวเป็น 0 → "วันนี้เคลียร์แล้ว ✓" ไม่ใช่โชว์ศูนย์เรียงกัน

สภาพคลัง  (ข้อมูลไว้ดู ไม่ใช่งานที่ต้องทำ)
  ต้องสั่งเพิ่ม n · ค้างเกิน 90 วัน n · มูลค่าสต็อก ฿n
```

- [x] แถวงานค้าง เรียงตามความเร่งด่วน กดแล้วไปหน้าที่กรองไว้ให้แล้ว — ทดสอบจริงด้วยออเดอร์ทดสอบ (insert แล้วลบทิ้ง) ยืนยันว่า "รอชำระส่วนที่เหลือ" พาไป `/admin/orders?status=awaiting_balance` ถูกแท็บจริง
  ต้องแก้ `admin/orders/page.tsx` เพิ่มเติม 2 จุดเพื่อให้ลิงก์กรองได้ถูก (ไม่มีในแผนเดิม แต่จำเป็น):
  - เพิ่มแท็บ `awaiting_balance` ที่หายไปจาก `TABS` (สถานะนี้มีจริงในฐานข้อมูลแต่ไม่เคยมีแท็บให้กด)
  - เพิ่มตัวกรองเสมือน `to_ship` (= `status in ('paid','packing')`) เพราะ "ชำระแล้ว รอส่ง" ในหน้างานวันนี้ต้องรวมสองสถานะ ไม่ใช่แค่ `paid` ตัวเดียว — อิงจากที่ `orders/[id]/page.tsx` เองก็ปฏิบัติกับสองสถานะนี้เป็นกลุ่มเดียวกันอยู่แล้ว (ปุ่ม "จัดส่ง")
  - ป้าย "ของเข้าแล้ว รอจ่ายคิว" ในร่างแผนเปลี่ยนเป็น **"สั่งจองรอของเข้า"** ให้ตรงกับตัวเลขที่ view คำนวณจริง (`preorders_waiting` = คิวที่ยังรอของเข้า ไม่ใช่ของเข้าแล้วรอจ่าย) ลิงก์ไปหน้า `/admin/preorders` ที่มีอยู่แล้ว
- [x] สถานะ "เคลียร์แล้ว" เมื่อไม่มีงาน — ทดสอบแล้วทั้งสองสถานะ (มีงานค้าง/เคลียร์แล้ว)
- [x] ย้ายข้อมูลคลังไปอยู่ล่าง
- [x] บัญชีที่ไม่มีสิทธิ์ดูต้นทุนต้องไม่เห็นบล็อกคลัง — คงเงื่อนไข `showStock`/`showCost` เดิมไว้ทุกจุด ทดสอบด้วยบัญชี packer แล้วไม่เห็นบล็อกสภาพคลังเลย (ตรงตามเดิม)

### 1.4 เพิ่ม design token ชุดหลังบ้าน

เติมใน `globals.css` — **ใช้สีและฟอนต์ชุดเดิม** เปลี่ยนแค่ความหนาแน่น

| | หน้าร้าน | หลังบ้าน |
|---|---|---|
| ตัวอักษรพื้น | 15px | 14px |
| ปุ่ม | สูง 48px | สูง 36px |
| ระยะห่างการ์ด | 14px | 10px |
| ความสูงแถวตาราง | — | 40px |

- [x] `.a-table` `.a-row` `.a-btn` `.a-card` `.a-tabs` — เพิ่มใน `globals.css` แล้ว (ใช้ `.th`/`.td` เดิมของหน้าร้านสำหรับเซลล์ตาราง ไม่สร้างซ้ำ)
- [x] ตรวจ contrast — ไม่มีสีใหม่ ใช้ `--ink`/`--ink-muted`/`--ink-faint`/`--on-green` ชุดเดิมที่ตรวจผ่าน WCAG AA ไว้แล้วใน `:root` (เปลี่ยนแค่ขนาด/ระยะห่าง ไม่เปลี่ยนสี)

### 1.5 ดึงคอมโพเนนต์ที่ซ้ำอยู่แล้วออกมาใช้ร่วม

ซ้ำเป๊ะจริงในหลายไฟล์ (เช็คด้วย grep แล้ว ไม่ใช่คาดเดา) — ดึงตอนนี้ก่อนรอบ 4 จะได้ไม่ต้องแก้ซ้ำทีละหน้า

- โครงตาราง (`thead className="bg-neutral-50"` + `.th`/`.td` + แถวว่าง) ซ้ำเป๊ะใน 8 ไฟล์:
  `books`, `books/[id]`, `notifications`, `orders`, `orders/[id]`, `preorders`, `reports`, `stock`
- `StatCard` ตอนนี้เป็นฟังก์ชันในไฟล์ `admin/page.tsx` — ย้ายออกมาให้หน้า "งานวันนี้" (1.3) เรียกใช้ได้ด้วย

- [x] `src/components/admin/Table.tsx` — โครงตารางร่วม (หัว/แถว/ข้อความตอนไม่มีข้อมูล) สร้างไว้แล้ว **ยังไม่ได้เอาไปแทนที่ทั้ง 8 ไฟล์** (นั่นคืองานรอบ 4 — โครงเดิมยังทำงานถูกต้อง ไม่รีบแก้ของที่ไม่พัง)
- [x] `src/components/admin/StatCard.tsx` — ย้ายออกมาจริง หน้า "งานวันนี้" (1.3) เรียกใช้แล้ว ใช้ token หลังบ้าน (`.a-card`) ด้วย
- [x] `src/components/admin/EmptyState.tsx` — สร้างไว้แล้ว ยังไม่ได้แทนที่ของเดิมใน favourites/notifications/categories (รอบ 4)
- [x] `src/components/admin/orderBadge.ts` — ชุดสีป้ายสถานะเฉพาะหลังบ้าน (`ADMIN_ORDER_STATUS_BADGE`) แยกจาก `src/lib/orderStatus.ts` ที่หน้าร้านใช้ตามข้อ 4 ในตารางข้อตกลง — **ยังไม่ได้เอาไปใช้แทนที่ `ORDER_STATUS_STYLE` ในหน้า orders/preorders** (รอบ 4 หรือรอบ 2 ตอนรื้อหน้าคำสั่งซื้อ)

**เสร็จรอบนี้เมื่อ** เปิดหลังบ้านแล้วรู้ทันทีว่าต้องทำอะไร โดยไม่ต้องกดเข้าไปดูทีละหน้า

---

## รอบ 2 — คำสั่งซื้อ

### 2.1 หน้ารายการแบบซ้าย-ขวา

ปัญหาที่แก้: กดเข้าออเดอร์ → ทำงาน → ย้อนกลับ → **เสียตำแหน่ง** → เลื่อนหาใหม่
มีสิบออเดอร์ก็วนสิบรอบ

| จอ | พฤติกรรม |
|---|---|
| ≥ 1280px | ซ้ายรายการ 380px · ขวารายละเอียด — เลือกแล้วรายการไม่หาย |
| < 1280px | กลับไปเป็นสองหน้าเหมือนเดิม (รายการ → กดเข้า → รายละเอียด) |

- [ ] แยกรายการออเดอร์เป็น component ใช้ซ้ำได้ทั้งสองโหมด
- [ ] แถบสถานะ (ทั้งหมด/รอชำระ/รอส่ง/รอของเข้า/ส่งแล้ว) ยังอยู่ครบ
- [ ] URL ต้องเปลี่ยนตามออเดอร์ที่เลือก — คัดลอกลิงก์ส่งให้กันได้
- [ ] ทำงานเสร็จแล้วรายการทางซ้ายต้องอัปเดตสถานะเอง

### 2.2 หน้ารายละเอียดสองคอลัมน์

`orders/[id]/page.tsx` ตอนนี้ 13.5 KB เรียงลงมาเป็นแถวเดียว

```
ซ้าย                    ขวา (ค้างไว้ตอนเลื่อน)
  รายการสินค้า            สรุปเงิน
  ที่อยู่จัดส่ง             ปุ่มดำเนินการ
  พัสดุ                   สลิป
  ใบเสร็จ
```

- [ ] แยกสองคอลัมน์บนจอกว้าง · เรียงลงมาเหมือนเดิมบนจอแคบ
- [ ] ปุ่มดำเนินการค้างไว้ ไม่ต้องเลื่อนหา
- [ ] แยก `OrderActions.tsx` (8.8 KB) ตามชนิดงาน ไม่ให้ปุ่มทุกแบบกองรวมกัน

**เสร็จรอบนี้เมื่อ** เคลียร์ออเดอร์สิบรายการติดกันได้โดยไม่ต้องกดย้อนกลับเลย

---

## รอบ 3 — คลัง

### 3.1 แยกสี่งานเป็นสามแท็บ

`stock/page.tsx` ตอนนี้ 12.5 KB ทำสี่งานในหน้าเดียว

| แท็บ | ตอบคำถาม |
|---|---|
| สต็อกคงเหลือ (ค่าเริ่มต้น) | ตอนนี้เหลืออะไรเท่าไหร่ ต้องสั่งอะไรเพิ่ม |
| ล็อตและต้นทุน | ล็อตไหนเข้ามาเมื่อไหร่ ต้นทุนเท่าไหร่ FIFO จะตัดตัวไหนก่อน |
| ความเคลื่อนไหว | เดือนนี้ขาย/รับเข้าเท่าไหร่ · กราฟรายวัน |

- [ ] แท็บจำสถานะไว้ใน URL — refresh แล้วยังอยู่แท็บเดิม
- [ ] ตัวกรองช่วงวันที่ใช้ร่วมกันทุกแท็บ

### 3.2 ฟอร์มเป็นแผงเลื่อน ไม่ใช่เปลี่ยนหน้า

- [ ] ปุ่ม **รับสินค้าเข้า** / **ปรับสต็อก** มุมขวาบน เปิดแผงเลื่อนจากขวา
- [ ] บันทึกเสร็จ ปิดแผง ตารางอัปเดตเอง โดยยังอยู่แท็บและตัวกรองเดิม
- [ ] **คงแผงคำนวณต้นทุนสดไว้** (`฿200 + ฿100÷2 = ฿250`) — ของดีที่มีอยู่แล้ว
- [ ] บนมือถือแผงกางเต็มจอแทนการเลื่อนจากข้าง
- [ ] ลบ `/admin/stock/receive` และ `/admin/stock/adjust` ออกจากเมนู แต่**คง route ไว้** เผื่อมีลิงก์เก่า

**เสร็จรอบนี้เมื่อ** รับของเข้า 5 ล็อตติดกันได้โดยไม่ต้องเปลี่ยนหน้าเลย

---

## รอบ 4 — หน้าที่เหลือ

### 4.1 รายงานกำไร — ปรับแค่ layout

**ไม่แยกหน้า ไม่ตัดเนื้อหา** ตามที่ตกลง

- [ ] ตัวเลขสรุป 5 ตัวเรียงเป็นแถวเดียวบนจอกว้าง (ตอนนี้กินที่แนวตั้งเยอะ)
- [ ] ตารางรายวัน/รายเล่ม/ค่าส่ง วางเป็นสองคอลัมน์บนจอกว้าง
- [ ] ปุ่ม export CSV ย้ายไปอยู่กับตัวกรองช่วงวันที่
- [ ] หัวตารางค้างไว้ตอนเลื่อน

### 4.2 หน้าที่เหลือ

- [ ] หนังสือ — ตารางหนาแน่นขึ้น ค้นหาค้างด้านบน
- [ ] หมวดหมู่ — แก้ในบรรทัดได้เลย
- [ ] ลูกค้าสนใจ
- [ ] แจ้งเตือน — กรองตามสถานะได้ (ตอนนี้ดูรวมอย่างเดียว)
- [ ] `forbidden` — ใช้ token ชุดใหม่

**เสร็จรอบนี้เมื่อ** ไม่เหลือ `teal-` `slate-` `gray-[0-9]` ในโฟลเดอร์ `admin/` เลย

---

## เช็คก่อนจบทุกรอบ

สถานะสำหรับรอบ 1 (ทำในเครื่องนี้แล้ว):

- [x] `npm run typecheck` — ผ่าน ไม่มี error
- [x] `npx opennextjs-cloudflare build` — build ผ่าน bundle รวมยังอยู่ที่ ~103 kB shared เท่าเดิม
- [x] เปิดจริงด้วยบัญชี owner — ทดสอบผ่าน `run-book-shop` driver จริง (ไม่ใช่แค่คาดเดา): ล็อกอิน → เห็นเมนู 5 กลุ่ม + หน้า "งานวันนี้" ทำงานถูกทั้งสถานะ "เคลียร์แล้ว" และตอนมีงานค้าง
- [x] เปิดด้วยบัญชีสิทธิ์ต่ำ — ทดสอบด้วย `packer@bookshop.local` เห็นแค่กลุ่ม "ขาย" กับ "ระบบ" ตรงตามสิทธิ์เดิม และจำลอง RLS ด้วย `set local role authenticated` ยืนยันว่า view งานค้างยังกรองสิทธิ์ถูก
- [x] ย่อจอเหลือ 375px — ทดสอบจริงด้วย driver: หน้าแรกไม่มีอะไรล้น เมนูยุบเป็นปุ่มแฮมเบอร์เกอร์ กดเปิด/กดลิงก์/ปิดเองทำงานถูก
- [ ] `db-push.cmd` — **ยังไม่รัน** ต้องให้คุณสั่งเอง (พุชขึ้น Supabase Cloud จริง)
- [ ] `deploy-check-and-push.cmd` แล้วรอ Cloudflare build — **ยังไม่รัน** ต้องให้คุณสั่งเอง (deploy ขึ้นจริง)

---

## ความคืบหน้า

| รอบ | สถานะ | เสร็จเมื่อ |
|---|---|---|
| 1 — โครงหลัก + งานวันนี้ | **โค้ดเสร็จแล้ว รอ push ขึ้น production** | ทดสอบในเครื่อง (local Supabase + dev server) ผ่านหมดแล้ว เหลือแค่ `db-push.cmd` + `deploy-check-and-push.cmd` ที่ต้องให้เจ้าของโปรเจกต์สั่งเอง |
| 2 — คำสั่งซื้อ | ยังไม่เริ่ม | |
| 3 — คลัง | ยังไม่เริ่ม | |
| 4 — หน้าที่เหลือ | ยังไม่เริ่ม | |

---

## งานค้างอื่นที่ไม่เกี่ยวกับ UX

ไม่อยู่ในแผนนี้ แต่จดไว้ไม่ให้ลืม

- [ ] **ขอ API key ใหม่จาก ShipPop** — คีย์เก่าเคยขึ้น git ลบออกจากประวัติไม่ได้
- [ ] สร้างบัญชี owner สำรอง — ตอนนี้มีคนเดียว
- [ ] เก็บเบอร์โทรเป็นตัวระบุตัวตนสำรอง — กันปัญหาบัญชีซ้ำถ้าเปลี่ยน OA อีก
