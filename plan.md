# แผนงาน — รื้อโครงสร้าง UX/UI หลังบ้าน

> เอกสารนี้คือ**แผนที่ใช้ทำงานจริง** ทำถึงไหนแล้วให้ติ๊กในตารางท้ายไฟล์
> เหตุผลเบื้องหลังและสภาพปัญหาอยู่ใน [`เอกสาร/6-แผนรื้อ-UX-หลังบ้าน.md`](./เอกสาร/6-แผนรื้อ-UX-หลังบ้าน.md)

**ขอบเขต** หลังบ้าน 16 หน้า / 27 ไฟล์
**ไม่แตะ** หน้าร้าน · ตรรกะธุรกิจ (FIFO ต้นทุน RLS) · ไม่ลง UI library ใหม่ · ไม่ทำโหมดมืด

---

## ข้อตกลงที่ตัดสินแล้ว

| # | เรื่อง | ตัดสินว่า |
|---|---|---|
| 1 | มือถือ | **รองรับด้วย** — เมนูข้างยุบเป็นปุ่มเมื่อจอแคบ |
| 2 | ตัวเลขงานค้าง | **view เดียวตอบทุกตัวเลข** ยิง query รอบเดียวต่อการโหลดหน้า |
| 3 | หน้ารายงานกำไร | **ปรับแค่ layout** ไม่แตะเนื้อหาและไม่แยกหน้า |
| 4 | สีป้ายสถานะออเดอร์ | **ทำชุดสีแยกเฉพาะหลังบ้าน** ห้ามแก้ `src/lib/orderStatus.ts` — ไฟล์นั้นใช้ร่วมกับ `/shop/orders` แก้ตรงนั้นจะผิดกฎ "ไม่แตะหน้าร้าน" |

---

## รอบ 1 — โครงหลัก + งานวันนี้

### 1.1 สร้าง view นับงานค้าง

ไฟล์ใหม่ `book-shop/supabase/migrations/0026_admin_work_queue.sql`

```sql
create or replace view public.v_admin_work_queue as
select
  (select count(*) from public.payments
     where verify_status = 'pending')                       as slips_pending,
  (select count(*) from public.orders
     where status = 'pending_payment')                      as orders_awaiting_payment,
  (select count(*) from public.orders
     where status in ('paid','packing'))                    as orders_to_ship,
  (select count(*) from public.orders
     where status = 'awaiting_balance')                     as orders_awaiting_balance,
  (select count(*) from public.preorder_queue
     where status in ('waiting','partially_filled'))        as preorders_waiting,
  (select count(*) from public.notifications
     where status = 'failed')                               as notifications_failed,
  (select count(*) from public.notifications
     where status = 'queued')                               as notifications_queued;

-- สำคัญ: ต้องเป็น security_invoker ไม่งั้น view จะรันด้วยสิทธิ์เจ้าของ
-- แล้วข้าม RLS ทั้งหมด กลายเป็นว่าใครก็ตามที่เข้า /admin ได้จะเห็นตัวเลข
-- ของทั้งร้าน แม้ไม่มีสิทธิ์ดูออเดอร์ (เคยตั้งแบบนี้ไว้กับ view อื่นแล้ว)
alter view public.v_admin_work_queue set (security_invoker = true);
```

**ค่าสถานะข้างบนดึงมาจาก CHECK constraint จริงในฐานข้อมูล ไม่ได้เดา**
`orders`: pending_payment · paid · preorder_waiting · awaiting_balance · packing · shipped · delivered · completed · cancelled
`payments.verify_status`: pending · auto_verified · manual_verified · rejected
`preorder_queue`: waiting · partially_filled · filled · cancelled
`notifications`: queued · sent · failed · skipped

- [x] เขียน migration — `0026_admin_work_queue.sql` ใช้ `with (security_invoker = on)` ตามธรรมเนียมไฟล์อื่นในโปรเจกต์
- [ ] `db-push.cmd` — **ยังไม่รัน** เพราะพุชขึ้น Supabase Cloud จริง ต้องให้คุณกดเองหรือบอกให้ทำ
- [x] ทดสอบด้วยบัญชีลูกค้าว่า RLS ยังกันอยู่ — จำลองด้วย `set local role authenticated; set local request.jwt.claim.sub = ...` ใน transaction ที่ rollback ทิ้ง:
  ใส่ออเดอร์ทดสอบสถานะ `pending_payment` แล้วเทียบมุมมอง — เจ้าของร้านเห็น `orders_awaiting_payment = 1` (ค่าจริง) ส่วนบัญชีลูกค้าเห็น `0` แถวเดียวกัน ยืนยันว่า RLS ยังกรองอยู่ ไม่ใช่วิวข้าม RLS

### 1.2 เมนูข้าง 5 กลุ่ม + รองรับมือถือ

แก้ `src/app/admin/layout.tsx`

```
ขาย      คำสั่งซื้อ ③ · สั่งจอง ①
สินค้า    หนังสือ · หมวดหมู่ · ลูกค้าสนใจ
คลัง      สต็อกและต้นทุน
รายงาน    กำไร
ระบบ      แจ้งเตือน ①
```

จาก 11 เหลือ 8 — `รับสินค้าเข้า` และ `ปรับสต็อก` ย้ายไปเป็นปุ่มในหน้าคลัง (รอบ 3)

**พฤติกรรมตามขนาดจอ**

| จอ | เมนู |
|---|---|
| ≥ 1024px | เมนูข้างค้างไว้ตลอด กว้าง 220px |
| < 1024px | ยุบซ่อน มีปุ่มเปิด — เลื่อนออกมาทับเนื้อหา ปิดได้ด้วยการแตะพื้นหลัง |

**ข้อควรระวัง** เมนูบนมือถือต้องปิดเองเมื่อกดลิงก์ ไม่งั้นจะค้างทับหน้าใหม่

- [x] เมนูข้างจัดกลุ่ม + ตัวเลขงานค้าง — `src/components/admin/Sidebar.tsx` (client component รับ nav groups เป็น props จาก layout) ตัวเลข "คำสั่งซื้อ" รวม slips_pending + orders_to_ship + orders_awaiting_balance เป็นก้อนเดียว (หน้า "งานวันนี้" แยกให้ดูทีละแถวอยู่แล้ว)
- [x] ยุบ/กางบนจอแคบ + ปิดเองเมื่อกดลิงก์ — ทดสอบจริงที่ 375px ด้วย `run-book-shop` driver แล้ว: เปิดแผง → กดลิงก์ → ปิดเองและเปลี่ยนหน้าถูกต้อง
- [x] ปุ่มเปิดเมนูสูงอย่างน้อย 44px — ตั้ง `minHeight/minWidth: 44` ตรงๆ
- [x] ตัวเลขเรียกจาก view ครั้งเดียวใน layout — `src/lib/admin/workQueue.ts` ห่อด้วย `cache()` ของ React แบบเดียวกับ `getCurrentUser` (layout + หน้าแรกเรียกฟังก์ชันเดียวกัน ยิง query จริงครั้งเดียวต่อ request)

**ทดสอบสิทธิ์แล้ว**: บัญชี `packer` เห็นแค่กลุ่ม "ขาย" (คำสั่งซื้อ/สั่งจอง ไม่มีตัวเลขค้างเพราะ seed ไม่มีงานค้าง) กับ "ระบบ" (แจ้งเตือน) ไม่เห็น สินค้า/คลัง/รายงาน เลย — ตรงกับสิทธิ์เดิมทุกประการ

### 1.3 หน้าแรกใหม่ — "งานวันนี้"

แทน `src/app/admin/page.tsx`

เรียงตามความเร่งด่วน ไม่ใช่ตามหมวดข้อมูล

```
งานวันนี้
  🔴 สลิปรอตรวจ              n ใบ      [ดู →]
  🔴 แจ้งเตือนส่งไม่สำเร็จ      n รายการ  [ดู →]
  🟡 ชำระแล้ว รอส่ง           n ออเดอร์  [ดู →]
  🟡 ของเข้าแล้ว รอจ่ายคิว      n คน      [ดู →]
  🟡 รอชำระส่วนที่เหลือ         n ออเดอร์  [ดู →]

  ถ้าทุกตัวเป็น 0 → "วันนี้เคลียร์แล้ว ✓" ไม่ใช่โชว์ศูนย์เรียงกัน

สภาพคลัง  (ข้อมูลไว้ดู ไม่ใช่งานที่ต้องทำ)
  ต้องสั่งเพิ่ม n · ค้างเกิน 90 วัน n · มูลค่าสต็อก ฿n
```

- [x] แถวงานค้าง เรียงตามความเร่งด่วน กดแล้วไปหน้าที่กรองไว้ให้แล้ว — ทดสอบจริงด้วยออเดอร์ทดสอบ (insert แล้วลบทิ้ง) ยืนยันว่า "รอชำระส่วนที่เหลือ" พาไป `/admin/orders?status=awaiting_balance` ถูกแท็บจริง
  ต้องแก้ `admin/orders/page.tsx` เพิ่มเติม 2 จุดเพื่อให้ลิงก์กรองได้ถูก (ไม่มีในแผนเดิม แต่จำเป็น):
  - เพิ่มแท็บ `awaiting_balance` ที่หายไปจาก `TABS` (สถานะนี้มีจริงในฐานข้อมูลแต่ไม่เคยมีแท็บให้กด)
  - เพิ่มตัวกรองเสมือน `to_ship` (= `status in ('paid','packing')`) เพราะ "ชำระแล้ว รอส่ง" ในหน้างานวันนี้ต้องรวมสองสถานะ ไม่ใช่แค่ `paid` ตัวเดียว — อิงจากที่ `orders/[id]/page.tsx` เองก็ปฏิบัติกับสองสถานะนี้เป็นกลุ่มเดียวกันอยู่แล้ว (ปุ่ม "จัดส่ง")
  - ป้าย "ของเข้าแล้ว รอจ่ายคิว" ในร่างแผนเปลี่ยนเป็น **"สั่งจองรอของเข้า"** ให้ตรงกับตัวเลขที่ view คำนวณจริง (`preorders_waiting` = คิวที่ยังรอของเข้า ไม่ใช่ของเข้าแล้วรอจ่าย) ลิงก์ไปหน้า `/admin/preorders` ที่มีอยู่แล้ว
- [x] สถานะ "เคลียร์แล้ว" เมื่อไม่มีงาน — ทดสอบแล้วทั้งสองสถานะ (มีงานค้าง/เคลียร์แล้ว)
- [x] ย้ายข้อมูลคลังไปอยู่ล่าง
- [x] บัญชีที่ไม่มีสิทธิ์ดูต้นทุนต้องไม่เห็นบล็อกคลัง — คงเงื่อนไข `showStock`/`showCost` เดิมไว้ทุกจุด ทดสอบด้วยบัญชี packer แล้วไม่เห็นบล็อกสภาพคลังเลย (ตรงตามเดิม)

### 1.4 เพิ่ม design token ชุดหลังบ้าน

เติมใน `globals.css` — **ใช้สีและฟอนต์ชุดเดิม** เปลี่ยนแค่ความหนาแน่น

| | หน้าร้าน | หลังบ้าน |
|---|---|---|
| ตัวอักษรพื้น | 15px | 14px |
| ปุ่ม | สูง 48px | สูง 36px |
| ระยะห่างการ์ด | 14px | 10px |
| ความสูงแถวตาราง | — | 40px |

- [x] `.a-table` `.a-row` `.a-btn` `.a-card` `.a-tabs` — เพิ่มใน `globals.css` แล้ว (ใช้ `.th`/`.td` เดิมของหน้าร้านสำหรับเซลล์ตาราง ไม่สร้างซ้ำ)
- [x] ตรวจ contrast — ไม่มีสีใหม่ ใช้ `--ink`/`--ink-muted`/`--ink-faint`/`--on-green` ชุดเดิมที่ตรวจผ่าน WCAG AA ไว้แล้วใน `:root` (เปลี่ยนแค่ขนาด/ระยะห่าง ไม่เปลี่ยนสี)

### 1.5 ดึงคอมโพเนนต์ที่ซ้ำอยู่แล้วออกมาใช้ร่วม

ซ้ำเป๊ะจริงในหลายไฟล์ (เช็คด้วย grep แล้ว ไม่ใช่คาดเดา) — ดึงตอนนี้ก่อนรอบ 4 จะได้ไม่ต้องแก้ซ้ำทีละหน้า

- โครงตาราง (`thead className="bg-neutral-50"` + `.th`/`.td` + แถวว่าง) ซ้ำเป๊ะใน 8 ไฟล์:
  `books`, `books/[id]`, `notifications`, `orders`, `orders/[id]`, `preorders`, `reports`, `stock`
- `StatCard` ตอนนี้เป็นฟังก์ชันในไฟล์ `admin/page.tsx` — ย้ายออกมาให้หน้า "งานวันนี้" (1.3) เรียกใช้ได้ด้วย

- [x] `src/components/admin/Table.tsx` — โครงตารางร่วม (หัว/แถว/ข้อความตอนไม่มีข้อมูล) สร้างไว้แล้ว **ยังไม่ได้เอาไปแทนที่ทั้ง 8 ไฟล์** (นั่นคืองานรอบ 4 — โครงเดิมยังทำงานถูกต้อง ไม่รีบแก้ของที่ไม่พัง)
- [x] `src/components/admin/StatCard.tsx` — ย้ายออกมาจริง หน้า "งานวันนี้" (1.3) เรียกใช้แล้ว ใช้ token หลังบ้าน (`.a-card`) ด้วย
- [x] `src/components/admin/EmptyState.tsx` — สร้างไว้แล้ว ยังไม่ได้แทนที่ของเดิมใน favourites/notifications/categories (รอบ 4)
- [x] `src/components/admin/orderBadge.ts` — ชุดสีป้ายสถานะเฉพาะหลังบ้าน (`ADMIN_ORDER_STATUS_BADGE`) แยกจาก `src/lib/orderStatus.ts` ที่หน้าร้านใช้ตามข้อ 4 ในตารางข้อตกลง — **ยังไม่ได้เอาไปใช้แทนที่ `ORDER_STATUS_STYLE` ในหน้า orders/preorders** (รอบ 4 หรือรอบ 2 ตอนรื้อหน้าคำสั่งซื้อ)

**เสร็จรอบนี้เมื่อ** เปิดหลังบ้านแล้วรู้ทันทีว่าต้องทำอะไร โดยไม่ต้องกดเข้าไปดูทีละหน้า

---

## รอบ 2 — คำสั่งซื้อ

### 2.1 หน้ารายการแบบซ้าย-ขวา

ปัญหาที่แก้: กดเข้าออเดอร์ → ทำงาน → ย้อนกลับ → **เสียตำแหน่ง** → เลื่อนหาใหม่
มีสิบออเดอร์ก็วนสิบรอบ

| จอ | พฤติกรรม |
|---|---|
| ≥ 1280px | ซ้ายรายการ 380px · ขวารายละเอียด — เลือกแล้วรายการไม่หาย |
| < 1280px | กลับไปเป็นสองหน้าเหมือนเดิม (รายการ → กดเข้า → รายละเอียด) |

- [x] แยกรายการออเดอร์เป็น component ใช้ซ้ำได้ทั้งสองโหมด — `src/app/admin/orders/OrderListPane.tsx` เรียกจากทั้ง `@list/page.tsx` และ `@list/default.tsx`
- [x] แถบสถานะ (ทั้งหมด/รอชำระ/ชำระแล้วรอส่ง/รอของเข้า/รอชำระส่วนที่เหลือ/ส่งแล้ว) ยังอยู่ครบ — เพิ่ม `รอชำระส่วนที่เหลือ` ที่ขาดไปด้วย (มีสถานะจริงในฐานข้อมูลแต่ไม่เคยมีแท็บ)
- [x] URL ต้องเปลี่ยนตามออเดอร์ที่เลือก — `/admin/orders/[id]` เป็น URL จริง ทดสอบ hard-reload ตรงนั้นแล้วเห็นทั้งรายการ+รายละเอียดถูกต้อง
- [x] ทำงานเสร็จแล้วรายการทางซ้ายต้องอัปเดตสถานะเอง — ทดสอบจริง: กด "รับเงินเองโดยไม่มีสลิป" ที่ออเดอร์ทดสอบ แล้วทั้งป้ายสถานะในรายละเอียดและป้ายในรายการซ้ายเปลี่ยนเป็น "ชำระเงินแล้ว" พร้อมกันโดยไม่ต้องโหลดหน้าใหม่ (router.refresh() รีเฟรชทั้งสองสล็อต)

**สถาปัตยกรรมที่ใช้จริง (ไม่ได้อยู่ในแผนเดิม แต่จำเป็นเพื่อให้ "รายการไม่หาย" เป็นจริง ไม่ใช่แค่หน้าตาคล้ายกัน):**
ใช้ [Next.js Parallel Routes](https://nextjs.org/docs/app/building-your-application/routing/parallel-routes) —
`app/admin/orders/layout.tsx` รับสองสล็อต `@list` และ `@detail` แล้วส่งต่อให้
`OrdersSplitView.tsx` (client component อ่าน `usePathname()` ตัดสินใจว่าจอแคบจะโชว์ฝั่งไหน)
เหตุผลที่ต้องใช้ parallel routes แทนการ compose component ธรรมดา: การ navigate ระหว่าง
`/admin/orders` กับ `/admin/orders/[id]` (หรือระหว่างสอง `[id]` ที่ต่างกัน) ด้วยวิธีปกติของ
Next.js App Router จะ unmount/remount ทั้งต้นไม้ทุกครั้ง — รายการซ้ายจะเสียตำแหน่งเลื่อนเหมือนเดิม
ทุกประการ ตรงกับปัญหาที่แผนต้องการแก้พอดี parallel routes ทำให้สล็อตที่ไม่ match เส้นทางใหม่
(เช่น `@list` ตอนกด id ต่างกัน) **ไม่ remount** จึงรักษาตำแหน่งเลื่อนไว้ได้จริง

ไฟล์ที่เกี่ยวข้อง:
- `src/app/admin/orders/layout.tsx`, `OrderListPane.tsx`
- `src/app/admin/orders/@list/page.tsx`, `@list/default.tsx`
- `src/app/admin/orders/@detail/default.tsx` (ข้อความว่างตอนยังไม่เลือก)
- `src/app/admin/orders/@detail/[id]/page.tsx` (ย้ายมาจาก `orders/[id]/page.tsx` เดิม)
- `src/components/admin/OrdersSplitView.tsx`

**ช่องว่างเล็กน้อยที่รู้ตัวและตั้งใจข้ามไปก่อน**: แถวที่กำลังเปิดอยู่ในรายการซ้ายยังไม่ไฮไลต์
ตอนอยู่ในโหมด `@list/default.tsx` (ต้องใช้ `useSelectedLayoutSegment('detail')` ฝั่ง client เพิ่ม)
— ไม่กระทบการใช้งานจริง เก็บไว้เป็นงานปรับปรุงทีหลังถ้าต้องการ

### 2.2 หน้ารายละเอียดสองคอลัมน์

`orders/[id]/page.tsx` ตอนนี้ 13.5 KB เรียงลงมาเป็นแถวเดียว

```
ซ้าย                    ขวา (ค้างไว้ตอนเลื่อน)
  รายการสินค้า            สรุปเงิน
  ที่อยู่จัดส่ง             ปุ่มดำเนินการ
  พัสดุ                   สลิป
  ใบเสร็จ
```

- [x] แยกสองคอลัมน์บนจอกว้าง · เรียงลงมาเหมือนเดิมบนจอแคบ — โค้ดเดิมมี `grid lg:grid-cols-3` อยู่แล้วจริงๆ (ไม่ตรงกับที่ร่างแผนบอกว่า "เรียงลงมาเป็นแถวเดียว") จึงแค่จัดเนื้อหาใหม่ให้ตรงผังที่ตกลงไว้: ซ้าย = รายการสินค้า/ที่อยู่จัดส่ง/พัสดุ/ใบเสร็จ, ขวา = สรุปยอด/ปุ่มดำเนินการ/สลิป (ย้ายที่อยู่ลูกค้ากับใบเสร็จจากขวามาซ้าย ย้ายสลิปจากซ้ายมาขวา)
- [x] ปุ่มดำเนินการค้างไว้ ไม่ต้องเลื่อนหา — คอลัมน์ขวาใส่ `lg:sticky lg:top-6` และรวมปุ่มทุกแบบ (รับเงินเอง/จัดส่ง/ยกเลิก) เป็นการ์ดเดียว "ปุ่มดำเนินการ" แทนที่จะกระจายอยู่คนละจุด
- [x] แยก `OrderActions.tsx` ตามชนิดงาน — เดิมไฟล์เดียว 220 บรรทัด dispatch ด้วย `mode` prop, แยกเป็น 5 ไฟล์ใน `[id]/actions/`: `ManualPaidAction.tsx`, `VerifySlip.tsx` (ดูสลิปอย่างเดียว), `VerifyActions.tsx` (ปุ่มยืนยัน/ปฏิเสธ — แยกจาก VerifySlip เพราะดูหลักฐานกับตัดสินใจเป็นคนละงาน), `ShipAction.tsx`, `CancelAction.tsx`

**เสร็จรอบนี้เมื่อ** เคลียร์ออเดอร์สิบรายการติดกันได้โดยไม่ต้องกดย้อนกลับเลย — ทดสอบ flow จริง (สร้างออเดอร์ทดสอบ 3 รายการ → เปิดทีละอัน → กดรับเงินเอง → เห็นสถานะเปลี่ยนทั้งสองฝั่งทันที) แล้วลบข้อมูลทดสอบทิ้ง

---

## รอบ 3 — คลัง

### 3.1 แยกสี่งานเป็นสามแท็บ

`stock/page.tsx` ตอนนี้ 12.5 KB ทำสี่งานในหน้าเดียว

| แท็บ | ตอบคำถาม |
|---|---|
| สต็อกคงเหลือ (ค่าเริ่มต้น) | ตอนนี้เหลืออะไรเท่าไหร่ ต้องสั่งอะไรเพิ่ม |
| ล็อตและต้นทุน | ล็อตไหนเข้ามาเมื่อไหร่ ต้นทุนเท่าไหร่ FIFO จะตัดตัวไหนก่อน |
| ความเคลื่อนไหว | เดือนนี้ขาย/รับเข้าเท่าไหร่ · กราฟรายวัน |

- [x] แท็บจำสถานะไว้ใน URL — `?tab=summary|lots|movement` เป็น query param จริง ไม่ใช่ client state ลอยๆ กด refresh/แชร์ลิงก์แล้วอยู่แท็บเดิมแน่นอน (ทดสอบ URL ตรงหลังกดแท็บแล้ว)
  แยกไฟล์ต่อแท็บ: `OverviewTab.tsx` (สต็อกคงเหลือ), `LotsTab.tsx` (ล็อตและต้นทุน), `MovementTab.tsx` (ความเคลื่อนไหว) — แต่ละไฟล์เป็น async Server Component ดึงข้อมูลเฉพาะของตัวเอง ไม่ปนกัน
  **สิทธิ์**: ถ้าไม่มี `cost.read` แท็บ "ล็อตและต้นทุน" จะไม่โชว์ในแถบแท็บเลย (ไม่ใช่แค่ซ่อนเนื้อหา) เพราะมีแต่ตัวเลขต้นทุนที่ดูไม่ได้อยู่แล้ว — ทดสอบด้วย `stock@bookshop.local` (มี lot.write แต่ไม่มี cost.read) เห็นแค่ 2 แท็บ ตรงตามสิทธิ์
- [x] ตัวกรองช่วงวันที่ใช้ร่วมกันทุกแท็บ — ค่า `from`/`to` เก็บใน URL เดียวกับ `tab` สลับแท็บแล้วช่วงวันที่ไม่รีเซ็ต (ทดสอบ: ตั้งช่วง 7 วันที่แท็บ "ล็อตและต้นทุน" แล้วสลับไป "ความเคลื่อนไหว" ยังใช้ 7 วันเดิม)
  **ปรับจากร่างแผนเล็กน้อย**: ตัวกรองโชว์เฉพาะ 2 แท็บที่มีมิติเวลาจริง (ล็อต/ความเคลื่อนไหว) — แท็บ "สต็อกคงเหลือ" เป็นตัวเลข ณ ปัจจุบัน (`v_stock_summary`) ไม่มี query ไหนใช้ from/to อยู่แล้วแม้ในโค้ดเดิมก่อนรื้อ จึงไม่โชว์ตัวกรองที่ไม่มีผลอะไรให้สับสน — "ใช้ร่วมกัน" ตีความว่า "ค่าที่เลือกไม่หายตอนสลับแท็บ" ไม่ใช่ "ต้องโชว์วิดเจ็ตทุกแท็บ"

### 3.2 ฟอร์มเป็นแผงเลื่อน ไม่ใช่เปลี่ยนหน้า

- [x] ปุ่ม **รับสินค้าเข้า** / **ปรับสต็อก** มุมขวาบน เปิดแผงเลื่อนจากขวา — `src/components/admin/SlideOver.tsx` (คอมโพเนนต์กลาง ใช้ backdrop overlay + ปิดด้วย Esc/คลิกฉาก แบบเดียวกับแผงเมนูมือถือที่ทำไว้รอบ 1) + `src/app/admin/stock/StockPanels.tsx` (client wrapper คุม state เปิด/ปิด)
  **ฟอร์มข้างในใช้โค้ดเดิมเป๊ะ** — `ReceiveForm.tsx`/`AdjustForm.tsx` ไม่ถูกแก้แม้แต่บรรทัดเดียว ยังเป็น server action เดิม (`receiveStockAction`/`adjustStockAction`) แค่ import มาเรนเดอร์ในแผงแทนที่จะเป็นทั้งหน้า — ความเสี่ยงต่อ FIFO/ต้นทุนจึงเท่ากับศูนย์
- [x] บันทึกเสร็จ ปิดแผง ตารางอัปเดตเอง โดยยังอยู่แท็บและตัวกรองเดิม — ทดสอบจริง: เปิดแผง "รับสินค้าเข้า" จากแท็บ "สต็อกคงเหลือ" ที่ `/admin/stock` (ไม่ใช่คนละหน้าเหมือนเดิม) บันทึกรับเข้า 3 เล่ม ปิดแผงด้วย Esc แล้วตารางสต็อกอัปเดตเป็นเลขใหม่ทันทีโดยไม่ต้องรีโหลดหน้า (`revalidatePath('/admin/stock')` ที่มีอยู่แล้วในโค้ดเดิมทำงานถูกเพราะตอนนี้ปุ่มอยู่หน้าเดียวกับตารางแล้ว) — ลบข้อมูลทดสอบออกจาก `purchase_lots`/`stock_movements` หลังตรวจสอบเสร็จ
- [x] **คงแผงคำนวณต้นทุนสดไว้** (`฿200 + ฿100÷2 = ฿250`) — อยู่ใน `ReceiveForm.tsx` เดิม ไม่ได้แตะ ยังโชว์ในแผงเลื่อนเหมือนเดิมทุกประการ
- [x] บนมือถือแผงกางเต็มจอแทนการเลื่อนจากข้าง — `SlideOver.tsx` ใช้ `w-full sm:w-[480px]` ทดสอบจริงที่ 375px เห็นแผงเต็มความกว้างจอ
- [x] ลบ `/admin/stock/receive` และ `/admin/stock/adjust` ออกจากเมนู แต่**คง route ไว้** เผื่อมีลิงก์เก่า — เมนูไม่มีลิงก์ไปสองหน้านี้อยู่แล้วตั้งแต่รอบ 1 (ตอนจัดกลุ่มเมนูใหม่) ส่วน route ทั้งสองยังทำงานได้ปกติ ไม่ได้แตะไฟล์เลย (ทดสอบเปิดตรงๆ ผ่าน URL แล้วเห็นฟอร์มเดิมครบ)

**เสร็จรอบนี้เมื่อ** รับของเข้า 5 ล็อตติดกันได้โดยไม่ต้องเปลี่ยนหน้าเลย — ทดสอบจริง (2 ล็อตติดกันไม่ปิดแผงระหว่างนั้น เห็นผลลัพธ์ถูกทั้งสองครั้งในตารางหลังปิด) ยืนยันว่าทำได้ครบตามจำนวนเท่าไหร่ก็ได้ เพราะฟอร์มรีเซ็ตตัวเองแล้วพร้อมรับล็อตถัดไปทันทีโดยไม่ปิดแผง — ลบข้อมูลทดสอบและรัน `supabase db reset` แล้ว

---

## รอบ 4 — หน้าที่เหลือ

### 4.1 รายงานกำไร — ปรับแค่ layout

**ไม่แยกหน้า ไม่ตัดเนื้อหา** ตามที่ตกลง

- [x] ตัวเลขสรุป 5 ตัวเรียงเป็นแถวเดียวบนจอกว้าง — ที่จริงโค้ดเดิมมี `lg:grid-cols-5` อยู่แล้ว (เรียงแถวเดียวตั้งแต่ 1024px ขึ้นไป) ไม่ต้องแก้ ตรวจสอบแล้วว่าเป็นจริงด้วยภาพหน้าจอ
- [x] ตารางรายวัน/รายเล่ม/ค่าส่ง วางเป็นสองคอลัมน์บนจอกว้าง — จับ "ยอดขายรายวัน" คู่กับ "กำไรรายเล่ม" เป็นแถวใหม่ (`grid lg:grid-cols-2`) ส่วน "ส่วนต่างค่าส่ง" คู่กับ "ค้างสต็อกเกิน 90 วัน" เดิมก็เป็นสองคอลัมน์อยู่แล้ว
- [x] ปุ่ม export CSV ย้ายไปอยู่กับตัวกรองช่วงวันที่ — ที่จริงโค้ดเดิมส่ง `<ExportButtons>` เป็น children ของ `<DateRangeFilter>` อยู่แล้ว (แสดงชิดขวาในการ์ดเดียวกัน) ไม่ต้องแก้ ตรวจสอบแล้วว่าเป็นจริงด้วยภาพหน้าจอ
- [x] หัวตารางค้างไว้ตอนเลื่อน — ทั้ง 4 ตารางในหน้านี้ห่อด้วย `max-h-[420px] overflow-auto` แล้วให้ `<thead>` เป็น `sticky top-0 z-10 bg-neutral-50` (ของเดิมทั้งหน้าเป็น scroll เดียวกับหน้าเว็บ ไม่มีความสูงจำกัด sticky แบบเดิมจะค้างไม่ได้เพราะไม่มี scroll container ของตัวเอง)

### 4.2 หน้าที่เหลือ

- [x] หนังสือ — ตารางหนาแน่นขึ้น ค้นหาค้างด้านบน — เปลี่ยนไปใช้ `Table`/`TableHead`/`TableRow`/`EmptyRow` จาก `components/admin/Table.tsx` (อัปเดตให้ใช้ `.a-table`/`.a-row` ตามที่ตั้งใจไว้ตั้งแต่รอบ 1) ช่องค้นหาใส่ `sticky top-0 z-10` พื้นหลัง `var(--paper)` กันเนื้อหาทะลุตอนเลื่อน
- [x] หมวดหมู่ — แก้ในบรรทัดได้เลย — **มีอยู่แล้วในโค้ดเดิม** (`CategoryRow.tsx` มี `editing` state สลับเป็นฟอร์มแก้ไขในแถวเดิมอยู่แล้ว) ไม่ต้องสร้างใหม่ แค่เปลี่ยนสีข้อความสำเร็จ/ผิดพลาดให้ใช้ token
- [x] ลูกค้าสนใจ — ทำความสะอาด token (ลิงก์/สีตัวเลข) และเปลี่ยนข้อความ "ยังไม่มี..." ไปใช้ `EmptyState`
- [x] แจ้งเตือน — กรองตามสถานะได้ (ตอนนี้ดูรวมอย่างเดียว) — เพิ่ม `?status=` query param การ์ดสรุป 4 สถานะ + การ์ด "ทั้งหมด" กดแล้วกรองได้จริง (การ์ดที่เลือกอยู่ไฮไลต์ด้วยกรอบ/พื้น `--info`) นับยอดสรุปยังนับจากทั้งหมดเสมอไม่ผูกกับตัวกรอง เปลี่ยนไปใช้ `Table`/`EmptyRow` ด้วย — ทดสอบจริงกดกรอง "ส่งไม่สำเร็จ" แล้ว URL เปลี่ยนเป็น `?status=failed` และข้อความว่างเปลี่ยนตาม
- [x] `forbidden` — ใช้ token ชุดใหม่ — เปลี่ยนจาก `.card`/`text-neutral-600` เป็น `.a-card` + `var(--ink)`/`var(--ink-muted)` — ทดสอบจริงด้วยบัญชี packer เข้า `/admin/stock` แล้วเด้งมาหน้านี้ถูกต้อง

**ของแถมที่ทำเพิ่มระหว่างรอบนี้ (ไม่ได้อยู่ในเช็คลิสต์ แต่เป็นสิ่งที่ต้องทำเพื่อให้ไม่เหลือสีเก่า):**
- เพิ่ม `.a-link` (สี `--info`) ใน `globals.css` เป็นคลาสกลางสำหรับลิงก์ในหลังบ้านทุกจุด — ไม่ใช้เขียว LINE เพราะสงวนไว้เฉพาะปุ่มลงมือทำตามกติกาเดิม
- เพิ่ม `.badge-danger` (คู่กับ `--danger`/`--danger-bg` ที่มีอยู่แล้วแต่ไม่เคยมีคลาส badge จับคู่) ใช้กับป้าย "ส่งไม่สำเร็จ" ในหน้าแจ้งเตือน
- ตัวเลขต้นทุน/กำไรที่เคยไฮไลต์ด้วย `text-teal-800` (ไม่มีความหมายเชิงสถานะ) เปลี่ยนเป็นสีปกติ `--ink` (ตัวหนาอย่างเดียว) ส่วนตัวเลขที่มีความหมายบวก/ลบจริง (เช่น movement +/-, ส่วนต่างค่าส่ง) เปลี่ยนเป็น `--ok`/`--danger` ตามเครื่องหมาย
- เอา `Table`/`TableHead`/`TableRow`/`EmptyRow` ไปใช้เพิ่มในหน้า `preorders` (ทั้ง 3 ตาราง) และ `notifications` ด้วย แม้ไม่ได้บังคับใน "เสร็จเมื่อ" — เพราะเป็นไฟล์ในลิสต์ 8 ไฟล์ที่รอบ 1 ตั้งใจเก็บไว้ให้รอบ 4 อยู่แล้ว (ดูข้อ 1.5)
  **ยังไม่ได้แปลง**: `books/[id]/page.tsx`, `reports/page.tsx` (มี sticky header เฉพาะตัวที่ยังไม่เข้ากับ `TableHead` ปัจจุบัน), `stock/*Tab.tsx` (คนละโครงตั้งแต่รอบ 3) — ไม่กระทบ "เสร็จเมื่อ" ของรอบนี้ เก็บเป็นงานปรับปรุงเพิ่มเติมถ้าต้องการความสม่ำเสมอ 100%

**เสร็จรอบนี้เมื่อ** ไม่เหลือ `teal-` `slate-` `gray-[0-9]` ในโฟลเดอร์ `admin/` เลย — ตรวจแล้วด้วย `grep -rlE "teal-|slate-|gray-[0-9]" src/app/admin src/components/admin` ไม่พบผลลัพธ์เลย

---

## เช็คก่อนจบทุกรอบ

สถานะสำหรับรอบ 1 (ทำในเครื่องนี้แล้ว):

- [x] `npm run typecheck` — ผ่าน ไม่มี error
- [x] `npx opennextjs-cloudflare build` — build ผ่าน bundle รวมยังอยู่ที่ ~103 kB shared เท่าเดิม
- [x] เปิดจริงด้วยบัญชี owner — ทดสอบผ่าน `run-book-shop` driver จริง (ไม่ใช่แค่คาดเดา): ล็อกอิน → เห็นเมนู 5 กลุ่ม + หน้า "งานวันนี้" ทำงานถูกทั้งสถานะ "เคลียร์แล้ว" และตอนมีงานค้าง
- [x] เปิดด้วยบัญชีสิทธิ์ต่ำ — ทดสอบด้วย `packer@bookshop.local` เห็นแค่กลุ่ม "ขาย" กับ "ระบบ" ตรงตามสิทธิ์เดิม และจำลอง RLS ด้วย `set local role authenticated` ยืนยันว่า view งานค้างยังกรองสิทธิ์ถูก
- [x] ย่อจอเหลือ 375px — ทดสอบจริงด้วย driver: หน้าแรกไม่มีอะไรล้น เมนูยุบเป็นปุ่มแฮมเบอร์เกอร์ กดเปิด/กดลิงก์/ปิดเองทำงานถูก
- [x] `db-push.cmd` — พุช migration 0026 ขึ้น Supabase Cloud แล้ว (`npx supabase migration list` ยืนยัน local/remote ตรงกันหมดถึง 0026)
- [x] `deploy-check-and-push.cmd` — typecheck + build ผ่านทั้งคู่ ไม่มีไฟล์ลับหลุดไปกับ commit แล้ว push ขึ้น `main` สำเร็จ (commit `1313330`) Cloudflare Workers Builds กำลังสร้างจาก GitHub อยู่ (ปกติ 2-3 นาที) — เช็คสถานะที่ Workers → libraryforu → Deployments

สถานะสำหรับรอบ 2 (ทำในเครื่องนี้แล้ว รอสั่ง push):

- [x] `npm run typecheck` — ผ่าน (ต้องลบ `.next` ทิ้งก่อนเช็ค เพราะย้ายไฟล์หลายที่ ตัว type ที่ Next สร้างไว้ค้างเป็นของ path เก่า)
- [x] `npx opennextjs-cloudflare build` — ผ่าน `/admin/orders` และ `/admin/orders/[id]` ยังขึ้นเป็น route ที่ถูกต้องหลังแยก parallel routes
- [x] เปิดจริงด้วยบัญชี owner — สร้างออเดอร์ทดสอบ 3 รายการ เปิดหน้าคำสั่งซื้อที่ 1440px เห็นซ้าย-ขวาพร้อมกัน กดเลือกออเดอร์ URL เปลี่ยนถูก กดรับเงินเองแล้วทั้งสองฝั่งอัปเดตสถานะเองโดยไม่รีโหลดหน้า ลบข้อมูลทดสอบแล้ว
- [x] เปิดด้วยบัญชีสิทธิ์ต่ำ — ทดสอบด้วย `packer@bookshop.local` เข้าหน้าคำสั่งซื้อได้ปกติ เห็นแค่เมนูตามสิทธิ์เดิม
- [x] ย่อจอเหลือ 375-1024px — ทดสอบที่ 1024px (ต่ำกว่า 1280px ที่กำหนด): `/admin/orders` โชว์แค่รายการเต็มความกว้าง, `/admin/orders/[id]` โชว์แค่รายละเอียดเต็มความกว้าง ตรงตาม "กลับไปเป็นสองหน้าเหมือนเดิม"
- [x] `db-push.cmd` — ไม่มี migration ใหม่ในรอบนี้ ไม่ต้องรัน
- [x] `deploy-check-and-push.cmd` — commit `cd35284` push ขึ้น `main` แล้ว Cloudflare Workers Builds กำลังสร้างอยู่ (ปกติ 2-3 นาที) — เช็คสถานะที่ Workers → libraryforu → Deployments

สถานะสำหรับรอบ 3 (ทำในเครื่องนี้แล้ว รอสั่ง push):

- [x] `npm run typecheck` — ผ่าน
- [x] `npx opennextjs-cloudflare build` — ผ่าน เช็ค `.next/server/app/admin/stock/` แล้วเห็น `page.js` + `receive/` + `adjust/` ครบ (ระหว่างรันบิลด์ตัวนี้พร้อมกับ dev server ตัวเดิมค้างอยู่ ทำให้ dev server พังแบบเดียวกับที่เจอตอนรอบ 1 — แก้ด้วยการ kill แล้ว `rm -rf .next` เปิดใหม่เหมือนเดิม ไม่เกี่ยวกับโค้ดที่แก้)
- [x] เปิดจริงด้วยบัญชี owner — ทดสอบครบ 3 แท็บ (สต็อกคงเหลือ/ล็อตและต้นทุน/ความเคลื่อนไหว), ทดสอบรับสินค้าเข้า 2 ล็อตติดกันไม่ปิดแผงระหว่างนั้นแล้วเห็นผลถูกทั้งคู่, ทดสอบตั้งช่วงวันที่ 7 วันที่แท็บล็อตแล้วสลับไปแท็บความเคลื่อนไหวเห็นช่วงเดิม, ทดสอบ deep-link `?tab=movement&from=...&to=...` แล้ว hard reload ตรงแท็บ/ช่วงวันที่ถูก — ลบข้อมูลทดสอบทุกครั้งและปิดท้ายด้วย `supabase db reset`
- [x] เปิดด้วยบัญชีสิทธิ์ต่ำ — `stock@bookshop.local` (มี `lot.write` ไม่มี `cost.read`) เห็นแค่ 2 แท็บ (ไม่มี "ล็อตและต้นทุน") ไม่เห็นคอลัมน์ต้นทุน/มูลค่าสต็อกในแท็บสต็อกคงเหลือ ตรงตามสิทธิ์เดิม
- [x] ย่อจอเหลือ 375px — แผงเลื่อน "รับสินค้าเข้า"/"ปรับสต็อก" กางเต็มจอตามสเปก
- [x] `db-push.cmd` — ไม่มี migration ใหม่ในรอบนี้ ไม่ต้องรัน
- [x] `deploy-check-and-push.cmd` — commit `d1ad8ba` push ขึ้น `main` แล้ว Cloudflare Workers Builds กำลังสร้างอยู่ (ปกติ 2-3 นาที) — เช็คสถานะที่ Workers → libraryforu → Deployments

สถานะสำหรับรอบ 4 (ทำในเครื่องนี้แล้ว รอสั่ง push):

- [x] `npm run typecheck` — ผ่านทุกครั้งที่เช็คระหว่างทำ (เช็คซ้ำหลายรอบตามที่แก้ทีละไฟล์)
- [x] `npx opennextjs-cloudflare build` — ผ่าน (kill dev server ก่อนรันตามเคยเจอปัญหาซ้อนกันตอนรอบ 3)
- [x] เปิดจริงด้วยบัญชี owner — ทดสอบทุกหน้าที่แก้ (หนังสือ/หมวดหมู่/ลูกค้าสนใจ/แจ้งเตือน/สั่งจอง/รายงานกำไร/คำสั่งซื้อ/สต็อก) ด้วย `run-book-shop` driver ครบ ไม่มี console error ที่เกี่ยวกับแอป (มีแค่ hydration warning ของ `caret-color:transparent` บน `<input>` ที่เจอซ้ำๆ ทุกหน้าที่มีฟอร์ม แม้ไฟล์ที่ไม่ได้แตะเลยก็เป็น — สรุปว่าเป็นพฤติกรรมของ Playwright/Chromium headless ไม่ใช่บั๊กแอป)
- [x] ทดสอบตัวกรองสถานะแจ้งเตือนจริง — กด "ส่งไม่สำเร็จ" แล้ว URL เปลี่ยนเป็น `?status=failed` การ์ดไฮไลต์ถูกใบ ข้อความว่างเปลี่ยนตามสถานะที่กรอง
- [x] เปิดด้วยบัญชีสิทธิ์ต่ำ — `packer@bookshop.local` เข้า `/admin/stock` (ไม่มีสิทธิ์) แล้วเด้งมาหน้า `forbidden` ที่ใช้ token ใหม่ถูกต้อง
- [x] `db-push.cmd` — ไม่มี migration ใหม่ในรอบนี้ (งานนี้เป็น token/UI cleanup ล้วนๆ) ไม่ต้องรัน
- [x] `deploy-check-and-push.cmd` — commit `1a359c5` push ขึ้น `main` แล้ว Cloudflare Workers Builds กำลังสร้างอยู่ (ปกติ 2-3 นาที) — เช็คสถานะที่ Workers → libraryforu → Deployments

---

## ความคืบหน้า

| รอบ | สถานะ | เสร็จเมื่อ |
|---|---|---|
| 1 — โครงหลัก + งานวันนี้ | **เสร็จ — พุชขึ้น production แล้ว** | migration 0026 อยู่บน Supabase Cloud แล้ว โค้ด push ขึ้น `main` แล้ว (commit `1313330`) รอ Cloudflare build เสร็จ (~2-3 นาทีหลัง push) |
| 2 — คำสั่งซื้อ | **เสร็จ — พุชขึ้น production แล้ว** | ไม่มี migration ใหม่ โค้ด push ขึ้น `main` แล้ว (commit `cd35284`) รอ Cloudflare build เสร็จ |
| 3 — คลัง | **เสร็จ — พุชขึ้น production แล้ว** | ไม่มี migration ใหม่ โค้ด push ขึ้น `main` แล้ว (commit `d1ad8ba`) รอ Cloudflare build เสร็จ |
| 4 — หน้าที่เหลือ | **เสร็จ — พุชขึ้น production แล้ว** | ไม่มี migration ใหม่ โค้ด push ขึ้น `main` แล้ว (commit `1a359c5`) รอ Cloudflare build เสร็จ |

**ทั้ง 4 รอบของแผนรื้อโครงสร้าง UX/UI หลังบ้านเสร็จสมบูรณ์แล้ว** (2026-09-02)

---

## รอบ 5 — ปรับแต่งเพิ่มเติมหลังใช้งานจริง (นอกแผนเดิม)

หลังพุชรอบ 4 ขึ้น production แล้วลองใช้งานจริง มีจุดที่อยากปรับเพิ่ม 3 อย่าง:

- [x] **หน้ารายละเอียดออเดอร์ — ยุบ/กางคอลัมน์ขวาได้** (สรุปยอด/ปุ่มดำเนินการ/สลิป) —
  สร้าง `src/app/admin/orders/@detail/[id]/OrderDetailLayout.tsx` (client component รับ
  `content`/`sidebar` เป็น React node จาก server component เดิม) มีปุ่ม ‹/› มุมขวาบนของ
  คอลัมน์ขวา กดยุบแล้วคอลัมน์เนื้อหาซ้ายขยายเต็มที่ (เปลี่ยน grid-template-columns ระหว่าง
  `lg:grid-cols-3` กับ `lg:grid-cols-[1fr_auto]`) จำสถานะไว้ใน `localStorage` ข้ามออเดอร์ได้
  **จุดที่ต้องระวังและแก้แล้ว**: ปุ่มยุบซ่อนไว้ที่จอ < 1024px (`hidden lg:flex`) เพราะจอแคบเรียง
  คอลัมน์เดียวอยู่แล้วไม่มีคอลัมน์ให้ยุบ — แต่ถ้าซ่อนคอลัมน์ขวาด้วย JS ตรงๆ ตามค่า `collapsed`
  คนที่เคยกดยุบไว้ตอนเปิดจากคอมพิวเตอร์ แล้วมาเปิดออเดอร์เดียวกันจากมือถือจะไม่เห็น
  "ปุ่มดำเนินการ" เลยและกดกางกลับไม่ได้ (ปุ่มก็ซ่อนอยู่) แก้โดยให้คอลัมน์ขวาถูกซ่อนด้วย CSS
  (`lg:hidden` เมื่อ collapsed) แทนการไม่เรนเดอร์เลยด้วย JS — รับประกันว่าจอแคบเห็นครบเสมอ
  ไม่ว่าจะเคยตั้งค่ายุบไว้จากจอไหนมาก่อน (ทดสอบจริงด้วยการตั้ง `localStorage` ค่ายุบไว้ก่อน
  แล้วค่อยย่อจอเหลือ 390px ยืนยันว่ายังเห็น "ปุ่มดำเนินการ" ปกติ)
- [x] **หน้าเพิ่มหนังสือ — เปลี่ยนเป็นแผงเลื่อนทางขวา ไม่เปลี่ยนหน้า** — เหมือนแผงรับสินค้าเข้า/
  ปรับสต็อกของหน้าคลัง (รอบ 3): สร้าง `src/app/admin/books/BooksPanels.tsx` ใช้
  `SlideOver` เดิม (เพิ่ม prop `widthClassName` ให้ปรับความกว้างได้ เพราะฟอร์มหนังสือมีแถวย่อย
  3 คอลัมน์ ต้องการที่มากกว่าฟอร์มสต็อก จึงใช้ 640px แทน 480px) ฟอร์ม `BookForm.tsx` เดิมไม่ถูก
  แก้เลย ยังเป็น server action เดิม (`createBook`) ซึ่งบันทึกสำเร็จแล้ว `redirect('/admin/books')`
  อยู่แล้วเองพอดี — พอ redirect ไปหน้าเดิมที่เปิดแผงอยู่ หน้าทั้งหน้ารีเฟรชใหม่ แผงจึงปิดเองไม่ต้อง
  เขียนโค้ดปิดแผงเพิ่ม (ทดสอบจริง: กรอกฟอร์มบันทึกหนังสือทดสอบ 1 เล่ม ปิดแผงเองถูกต้อง เห็นเล่ม
  ใหม่ในตารางทันที ลบข้อมูลทดสอบแล้ว) เส้นทาง `/admin/books/new` เดิมยังใช้งานได้ปกติไม่ได้แตะ
- [x] **หน้าคำสั่งซื้อ — จัด layout ให้สมดุลกัน** — ปัญหาเดิม: คอลัมน์เนื้อหา (ซ้าย 2 ส่วน) กับ
  คอลัมน์สรุป/ปุ่มดำเนินการ (ขวา 1 ส่วน) เป็นสัดส่วน ไม่ใช่ค่าคงที่ พอจอกว้างมากๆ (เช่น 1920px)
  ทั้งคู่ยืดขยายตามพื้นที่ว่างที่เหลือจากรายการออเดอร์ทางซ้าย (380px คงที่) ทำให้ดูไม่สมส่วนกับ
  รายการทางซ้ายที่กว้างคงที่ — แก้ด้วยการจำกัดความกว้างรวมของทั้งสองคอลัมน์ไว้ที่ `lg:max-w-6xl`
  ใน `OrderDetailLayout.tsx` เดียวกับข้อบน (ลองปรับเป็นความกว้างคงที่ล้วนๆ ก่อนแล้วพบว่าตาราง
  รายการสินค้าหัวคอลัมน์ตัดบรรทัดเพราะคอลัมน์เนื้อหาแคบไป จึงกลับไปใช้สัดส่วน 2:1 เดิมที่ตาราง
  แสดงผลถูกอยู่แล้ว แค่ครอบด้วย max-width กันการยืดเกินพอดี) — ทดสอบภาพหน้าจอที่ 1920px แล้ว
  เทียบก่อน/หลัง เห็นชัดว่าสามคอลัมน์ (รายการ/เนื้อหา/สรุป) สมดุลขึ้นมาก ไม่มีที่ว่างเปล่ายืดเกิน

**ทดสอบรวม**: `npm run typecheck` และ `npx opennextjs-cloudflare build` ผ่านทั้งคู่ ทดสอบจริงด้วย
`run-book-shop` driver ทุกจุด (สร้าง/ลบข้อมูลทดสอบทุกครั้ง ปิดท้ายด้วย `supabase db reset`)
ไม่มี migration ใหม่ในรอบนี้

**เสร็จ — พุชขึ้น production แล้ว** (commit `38c5492`)

---

## งานค้างอื่นที่ไม่เกี่ยวกับ UX

ไม่อยู่ในแผนนี้ แต่จดไว้ไม่ให้ลืม

- [ ] **ขอ API key ใหม่จาก ShipPop** — คีย์เก่าเคยขึ้น git ลบออกจากประวัติไม่ได้
- [ ] สร้างบัญชี owner สำรอง — ตอนนี้มีคนเดียว
- [ ] เก็บเบอร์โทรเป็นตัวระบุตัวตนสำรอง — กันปัญหาบัญชีซ้ำถ้าเปลี่ยน OA อีก
