# วิธีติดตั้ง LINE Seed Sans TH

ใช้เวลาประมาณ 3 นาที ทำครั้งเดียวจบ

ผมโหลดไฟล์ฟอนต์มาให้ไม่ได้ เพราะ `seed.line.me` อยู่นอกรายชื่อเว็บที่เครื่องมือของผม
ดึงข้อมูลได้ และไฟล์ที่ได้เป็น zip ซึ่งเป็นไฟล์ไบนารี ไม่ใช่ข้อความ

**ยังไม่ต้องทำตอนนี้ก็ได้** — เว็บใช้ IBM Plex Sans Thai อยู่แล้วและทำงานได้ปกติ
ทำเมื่ออยากเห็นว่า LINE Seed ต่างจากตัวปัจจุบันแค่ไหน

---

## ขั้นตอน

**1. โหลดฟอนต์**

เปิด https://seed.line.me แล้วกดปุ่ม Download เลือก **LINE Seed TH**
จะได้ไฟล์ `LINE_Seed_Sans_TH.zip`

**2. แตกไฟล์**

ข้างในจะมีไฟล์นามสกุล `.woff2` และ `.ttf` หลายน้ำหนัก
เราใช้แค่สองตัวคือน้ำหนัก Regular กับ Bold

**3. สร้างโฟลเดอร์แล้ววางไฟล์**

สร้างโฟลเดอร์ตามนี้ (ถ้ายังไม่มี)

```
book-shop\public\fonts\
```

แล้วก๊อป **ไฟล์ .woff2** สองตัวนี้เข้าไป

| ไฟล์ที่ต้องใช้ | ชื่อที่ต้องเป็น |
| --- | --- |
| ตัวธรรมดา | `LINESeedSansTH_W_Rg.woff2` |
| ตัวหนา | `LINESeedSansTH_W_Bd.woff2` |

ชื่อไฟล์ต้องตรงเป๊ะรวมถึงตัวพิมพ์ใหญ่พิมพ์เล็ก เพราะเซิร์ฟเวอร์ที่ Netlify
แยกตัวพิมพ์ใหญ่เล็ก ต่างจาก Windows ที่ไม่แยก — ถ้าชื่อผิดจะใช้ได้บนเครื่องคุณ
แต่พอขึ้นเว็บจริงฟอนต์จะหายไปเฉยๆ โดยไม่มี error อะไรบอก

**ถ้าใน zip มีแต่ `.ttf` ไม่มี `.woff2`** ให้แปลงที่ https://cloudconvert.com/ttf-to-woff2
woff2 เล็กกว่า ttf ประมาณ 40% ซึ่งสำคัญมากเพราะลูกค้าโหลดผ่านเน็ตมือถือ

**4. ดูผล**

รัน `npm run dev` ในโฟลเดอร์ `book-shop` แล้วเปิด

```
http://localhost:3000/preview/fonts
```

จะเห็นสองคอลัมน์เทียบกัน ถ้าเหมือนกันเป๊ะแปลว่าไฟล์ยังไม่เข้าที่
ให้กลับไปเช็คชื่อไฟล์กับตำแหน่งโฟลเดอร์อีกรอบ

**เปิดบนมือถือด้วย** — หลัง deploy แล้วเข้า
`https://libraryforu.netlify.app/preview/fonts` ผ่านแอป LINE
เพราะฟอนต์ไทยตัดสินจากจอคอมไม่ได้ วรรณยุกต์เล็กๆ ที่บนคอมดูคม
พอลงมือถือกลายเป็นจุดเบลอได้

---

## เรื่องลิขสิทธิ์

LINE Seed อยู่ภายใต้ SIL Open Font License 1.1 ใช้เชิงพาณิชย์ได้ฟรี
ห้ามอย่างเดียวคือเอาไฟล์ฟอนต์ไปขายต่อ

LINE แนะนำ (ไม่ได้บังคับ) ให้ระบุที่มาของฟอนต์ไว้ในเว็บ
ถ้าจะทำ ใส่บรรทัดเดียวท้ายหน้าร้านว่า

> ฟอนต์ LINE Seed โดย LY Corporation

---

## เมื่อเลือกฟอนต์ได้แล้ว

บอกผมว่าเลือกตัวไหน ผมจะเก็บกวาดให้

- ลบหน้า `/preview/fonts` ทิ้ง
- ถ้าเลือก LINE Seed จะถอน IBM Plex ออกเพื่อไม่ให้ลูกค้าโหลดฟอนต์ที่ไม่ได้ใช้
- ถ้าเลือก IBM Plex จะลบบล็อก `@font-face` ของ LINE Seed ออกจาก `globals.css`
